---
export type Props = {
	class?: string;
}

const { class: className } = Astro.props;
---

<span class=`text-[0.813rem] leading-4.5 tracking-[0.025rem] font-ibm-plex-sans ${className}`>
	<slot />
</span>
