---
export type Props = {
	italic?: boolean;
	class?: string;
}

const { italic, class: className } = Astro.props;
---

<h2 class=`text-[1.375rem] leading-8 font-ibm-plex-serif ${italic && "italic"} ${className}`>
	<slot />
</h2>
